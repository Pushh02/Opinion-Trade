// import zod from "zod";
import { responsePayloadType } from "./responses/responsestypes";

// export const signUpSchema = zod.object({
//     name: zod.string().min(2).max(20),
//     email: zod.string().min(6).max(50),
//     password: zod.string().min(8).max(50),
// });

// export const signInSchema = zod.object({
//     email: zod.string().min(6).max(50),
//     password: zod.string().min(8).max(50),
// });

export { responsePayloadType }